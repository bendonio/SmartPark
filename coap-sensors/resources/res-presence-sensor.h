#include <stdbool.h>
/**
 * \brief                  Activate the resource
 */
void activate_res_presence_sensor(void);

/**
 * \brief                  Switch state of the resource occuiped/free
 */
void car_arrived_left(void);

/**
 * \brief                  Check if park slot is occupied
 */
bool isOccupied(void);